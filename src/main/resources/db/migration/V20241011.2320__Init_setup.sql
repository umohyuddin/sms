---- Create table for addresses
--CREATE TABLE addresses (
--    address_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    address_details VARCHAR2(255)
--);
--
---- Create table for schools
--CREATE TABLE schools (
--    school_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    school_name VARCHAR2(100) NOT NULL,
--    school_principal VARCHAR2(100),
--    address_id NUMBER,
--    FOREIGN KEY (address_id) REFERENCES addresses(address_id)
--);
--
---- Create table for branches
--CREATE TABLE branches (
--    branch_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    branch_name VARCHAR2(100) NOT NULL,
--    school_id NUMBER,
--    FOREIGN KEY (school_id) REFERENCES schools(school_id)
--);
--
---- Create table for campuses
--CREATE TABLE campuses (
--    campus_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    campus_name VARCHAR2(100) NOT NULL,
--    branch_id NUMBER,
--    FOREIGN KEY (branch_id) REFERENCES branches(branch_id)
--);
--
---- Create table for teachers
--CREATE TABLE teachers (
--    teacher_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    first_name VARCHAR2(50),
--    middle_name VARCHAR2(50),
--    last_name VARCHAR2(50),
--    school_id NUMBER,
--    FOREIGN KEY (school_id) REFERENCES schools(school_id)
--);
--
---- Create table for subjects
--CREATE TABLE subjects (
--    subject_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    subject_name VARCHAR2(100)
--);
--
---- Create table for classes
--CREATE TABLE classes (
--    class_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    class_name VARCHAR2(100),
--    class_code VARCHAR2(20),
--    date_from DATE,
--    date_to DATE,
--    subject_id NUMBER,
--    teacher_id NUMBER,
--    FOREIGN KEY (subject_id) REFERENCES subjects(subject_id),
--    FOREIGN KEY (teacher_id) REFERENCES teachers(teacher_id)
--);
--
---- Create table for students
--CREATE TABLE students (
--    student_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    first_name VARCHAR2(50),
--    middle_name VARCHAR2(50),
--    last_name VARCHAR2(50),
--    gender VARCHAR2(10),
--    date_of_birth DATE,
--    other_student_details VARCHAR2(255)
--);
--
---- Create table for student_addresses
--CREATE TABLE student_addresses (
--    student_id NUMBER,
--    address_id NUMBER,
--    date_from DATE,
--    date_to DATE,
--    address_details VARCHAR2(255),
--    PRIMARY KEY (student_id, address_id),
--    FOREIGN KEY (student_id) REFERENCES students(student_id),
--    FOREIGN KEY (address_id) REFERENCES addresses(address_id)
--);
--
---- Create table for student_parents
--CREATE TABLE student_parents (
--    student_id NUMBER,
--    parent_id NUMBER,
--    PRIMARY KEY (student_id, parent_id),
--    FOREIGN KEY (student_id) REFERENCES students(student_id)
--);
--
---- Create table for parents
--CREATE TABLE parents (
--    parent_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    first_name VARCHAR2(50),
--    middle_name VARCHAR2(50),
--    last_name VARCHAR2(50),
--    gender VARCHAR2(10),
--    other_parent_details VARCHAR2(255)
--);
--
---- Create table for families
--CREATE TABLE families (
--    family_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    head_of_family_parent_id NUMBER,
--    family_name VARCHAR2(100),
--    FOREIGN KEY (head_of_family_parent_id) REFERENCES parents(parent_id)
--);
--
---- Create table for family_members
--CREATE TABLE family_members (
--    family_member_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    family_id NUMBER,
--    parent_or_student_member VARCHAR2(50),
--    parent_id NUMBER,
--    student_id NUMBER,
--    FOREIGN KEY (family_id) REFERENCES families(family_id),
--    FOREIGN KEY (parent_id) REFERENCES parents(parent_id),
--    FOREIGN KEY (student_id) REFERENCES students(student_id)
--);
--
---- Create table for homework
--CREATE TABLE homework (
--    homework_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    student_id NUMBER,
--    date_created DATE,
--    homework_content VARCHAR2(255),
--    other_homework_details VARCHAR2(255),
--    FOREIGN KEY (student_id) REFERENCES students(student_id)
--);
--
---- Create table for reports
--CREATE TABLE reports (
--    report_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    student_id NUMBER,
--    date_created DATE,
--    report_content VARCHAR2(255),
--    teachers_comments VARCHAR2(255),
--    other_report_details VARCHAR2(255),
--    FOREIGN KEY (student_id) REFERENCES students(student_id)
--);
--
---- Create table for student_classes
--CREATE TABLE student_classes (
--    student_id NUMBER,
--    class_id NUMBER,
--    date_from DATE,
--    date_to DATE,
--    PRIMARY KEY (student_id, class_id),
--    FOREIGN KEY (student_id) REFERENCES students(student_id),
--    FOREIGN KEY (class_id) REFERENCES classes(class_id)
--);